# MySQL MCP Server - RUST
> lets see where this will take us all

### Create a container
using docker
```
docker build --platform linux/amd64 -t rust-mcp-mysql:latest -f dockerfile.mysql_mcp_rust .
```
